# Vercel-safe base dependencies (this is the file Vercel's Python builder
# installs, because it sits at the backend/ project root).
#
# torch, scikit-learn, uvicorn, and websockets are intentionally NOT here:
# torch's Linux wheel alone pulls ~4GB of CUDA libraries and blows past
# Vercel's 250MB function limit, and the serverless runtime provides its own
# server (no uvicorn) and can't hold WebSockets open. The affect estimator
# has a documented interpretable rule-based fallback for when PyTorch is
# absent (see /ml/model-card's live_inference_method field).
#
# For the FULL stack locally or in Docker (uvicorn server, WebSocket routes,
# trained PyTorch MLP, and training), install requirements-local.txt instead.
fastapi==0.111.0
sqlmodel==0.0.19
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
numpy==1.26.4
noise==1.2.2
python-dotenv==1.0.1
